// Configure Javadoc
javadoc {
    options {
        title = project.artifactName
        header "${project.artifactName} ${rootProject.version}"
        author true
        links 'https://javadoc.io/doc/com.leakyabstractions/result-api/' + libs.versions.result.api.get()
    }
    configure(options) {
        tags('apiNote:a:API Note:')
    }
}
